// this import triggers autoprefixer to use postcss.config.js from node_modules folder
import 'draft-js-inline-toolbar-plugin/lib/plugin.css';

document.addEventListener('DOMContentLoaded', () => {
    console.log('ready!');
});
